# Run `ghostty +show-config --default --docs` to view a list of
# all available config options and their default values.
# Additionally, each config option is also explained in detail
# on Ghostty's website, at https://ghostty.org/docs/config.

# first is 'correct', second is nerd font
# font-family = monofur
font-family = "Monofur"

background = 131213
foreground = f15c99
selection-background = 472234
selection-foreground = 131213
# {# rcol(colors::palette.color0)  #}
palette = 0=#272822
# {# rcol(colors::palette.color1)  #}
palette = 1=#f51c99   
# {# rcol(colors::palette.color2)  #}
palette = 2=#1cf599   
# {# rcol(colors::palette.color3)  #}
palette = 3=#f5991c   
# {# rcol(colors::palette.color4)  #}
palette = 4=#1c99f5   
# {# rcol(colors::palette.color5)  #}
palette = 5=#991cf5   
# {# rcol(colors::palette.color6)  #}
palette = 6=#00ffff   
# {# rcol(colors::palette.color7)  #}
palette = 7=#f8f8f2   
# {# rcol(colors::palette.color8)  #}
palette = 8=#75715e   
# {# rcol(colors::palette.color9)  #}
palette = 9=#f92672   
# {# rcol(colors::palette.color10) #}
palette = 10=#a6e22e  
# {# rcol(colors::palette.color11) #}
palette = 11=#f4bf75  
# {# rcol(colors::palette.color12) #}
palette = 12=#66d9ef  
# {# rcol(colors::palette.color13) #}
palette = 13=#ae81ff  
# {# rcol(colors::palette.color14) #}
palette = 14=#a1efe4  
# {# rcol(colors::palette.color15) #}
palette = 15=#f9f8f5  

background-opacity = 0.7
background-blur = true

window-theme = ghostty 
window-decoration = server
